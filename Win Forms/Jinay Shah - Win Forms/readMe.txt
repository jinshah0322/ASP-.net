Steps of implementation

1) Run the EmployeeDBScript file to generate the Database which contains all the tables.
2) Open the WinForm folder which will contain WinForm.sln file in visual studio.
3) Start the project by clicking start button or  with "ctrl+F5" command in visual studio.
4) On running the project windows forms will be open where you will be able to perform crud operations on winform.